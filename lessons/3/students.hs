null :: [a] -> Bool
null = _

head :: [a] -> a
head = _

tail :: [a] -> [a]
tail = _

-- | Return the first integer in the list, plus one, if there is one.
-- Otherwise return zero.
plusOne :: [Int] -> Int
plusOne = _

-- | Add together the first two elements of the list.
-- If there's only one element, just return it.
-- If there's nothing, just return zero.
addFirstTwo :: [Int] -> Int
addFirstTwo = _

length :: [a] -> Int
length = _

zip :: [a] -> [b] -> [(a, b)]
zip = _
